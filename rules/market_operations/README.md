# Правила рыночных операций

Здесь хранятся общие декларативные проверки сделок, котировок, лимитов,
подтверждений и расчётов. Предметные поля и формулы задаются в YAML-правилах,
а не в Python-коде ядра. Агент запускает правило, когда в `data/` найдены все его `source_ids`.
