[build-system]
# TODO: выбрать backend сборки при переходе к реализации.
requires = []
build-backend = ""

[project]
name = "audit-insight-agent"
version = "0.0.0"
description = "Каркас автономного аудиторского ядра для Ouroboros"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

# TODO: после реализации CLI зарегистрировать entry point audit-insight.
# [project.scripts]
# audit-insight = "audit_insight_agent.cli:main"

# TODO: добавить настройки линтера, форматтера, type checker и test runner.
